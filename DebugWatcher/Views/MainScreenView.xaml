<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" x:Class="DebugWatcher.MainScreenView" 
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="600" d:DesignWidth="800">
	<Grid>
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="HideShowOptionsPanel">
				<VisualStateGroup.Transitions>
					<VisualTransition GeneratedDuration="0:0:0.3"/>
				</VisualStateGroup.Transitions>
				<VisualState x:Name="ShowOptions">
					<Storyboard>
						<DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Storyboard.TargetName="OptionsPanel">
							<EasingDoubleKeyFrame KeyTime="0" Value="-311.112"/>
						</DoubleAnimationUsingKeyFrames>
					</Storyboard>
				</VisualState>
				<VisualState x:Name="HideOptions"/>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
		<Grid.RowDefinitions>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<TabControl x:Name="ChannelsTabControl">
			<TabItem Header="Requests" x:Name="RequestsHeader">
				<ListBox Background="White" Foreground="Black" FontFamily="Consolas"  x:Name="RequestsOutputBox" Margin="10">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Foreground="Black" Text="{Binding}"/>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</TabItem>
			<TabItem Header="Debug" x:Name="DebugHeader">
				<ListBox Background="White" Foreground="Black" FontFamily="Consolas"  x:Name="DebugOutputBox" Margin="10">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Foreground="Black" Text="{Binding}"/>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</TabItem>
			<TabItem Header="Exceptions" x:Name="ExceptionsHeader" Foreground="#FFC9C7C7">
				<ListBox Background="White" Foreground="Black" FontFamily="Consolas"  x:Name="ExceptionsOutputBox" Margin="10">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Foreground="Black" Text="{Binding}"/>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</TabItem>
			<StackPanel x:Name="ShowOptionsButtonPanel" Margin="634,-34.473,4,547" Orientation="Horizontal" ZIndex="100">
				<Button x:Name="ShowOptionsButton" Content="" Margin="0" Style="{DynamicResource WhiteBGArrowButton}" Height="35" Width="35" HorizontalAlignment="Left" VerticalAlignment="Top">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Click">
							<ei:GoToStateAction StateName="ShowOptions"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
				</Button>
				<TextBlock TextWrapping="Wrap" Text="options" Margin="10,0,0,0" FontSize="27" Foreground="#FFC9C7C7"/>
			</StackPanel>
		</TabControl>
		<StackPanel x:Name="OptionsPanel" HorizontalAlignment="Left" Height="600" Margin="811.928,0,-312.375,0" VerticalAlignment="Top" Width="300.447" Background="Black" RenderTransformOrigin="0.5,0.5">
			<StackPanel.RenderTransform>
				<TransformGroup>
					<ScaleTransform/>
					<SkewTransform/>
					<RotateTransform/>
					<TranslateTransform/>
				</TransformGroup>
			</StackPanel.RenderTransform>
			<StackPanel x:Name="HideOptionsButtonPanel" Orientation="Horizontal" Margin="25,12,0,0">
				<Button Content="" HorizontalAlignment="Left" Height="35" Style="{DynamicResource ArrowButtonStyle}" VerticalAlignment="Top" Width="35">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Click">
							<ei:GoToStateAction StateName="HideOptions"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
				</Button>
				<TextBlock Text="options"
					Foreground="Gainsboro"
					Margin="10,0,0,0"
					FontSize="27"/>
			</StackPanel>
		</StackPanel>
     

	</Grid>
</UserControl>
